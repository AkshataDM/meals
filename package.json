{
  "name": "meal-planner",
  "version": "1.0.0",
  "description": "A weekly meal planning application deployed as a single Cloudflare Worker",
  "private": true,
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {}
} 